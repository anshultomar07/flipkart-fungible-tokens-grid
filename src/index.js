import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(

  // To Run the website follow the commands in the terminal
  // 1. npm install
  // 2. npm start

  <React.StrictMode>
    <App />
  </React.StrictMode>
);
